name: CI
on: push

jobs:
  test:
    runs-on: [ code-default ]
    container: art.sec.samsung.net/docker-remote/alpine:3.15
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
    - run: npm install
    - run: npm test
